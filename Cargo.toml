[package]
name = "ollagram"
edition = "2024"

[dependencies]
ollama-rs = {version = "0.3"}
teloxide = {version = "0.17", features = ["macros"]}
tokio = { version = "1.39", features = ["rt-multi-thread", "macros"] }
serde = {version = "1.0", features = ["derive"]}
