This memory note is too long (max {max_size} characters).
Rewrite it shorter. Drop old and unimportant details. Keep the most useful facts.
Plain text only. Stay under {max_size} characters.

{summary}
